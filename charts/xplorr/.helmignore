# Nothing here belongs in the packaged chart a customer downloads.
#
# Not *.tgz: charts/opencost-*.tgz is the OpenCost dependency, and ignoring
# it packages a chart that declares OpenCost and does not carry it, which
# fails at package time with "found in Chart.yaml, but missing in charts/".
.DS_Store
.git/
.gitignore
